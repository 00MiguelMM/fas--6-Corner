// Componentes básicos de React Native
import { View, Text } from 'react-native';

// Pantalla de detalle de un objetivo concreto (ruta dinámica /objetivos/[id])
export default function ObjetivoDetalleScreen() {
  return (
    // Contenedor provisional del detalle
    <View>
      {/* Placeholder hasta cargar datos del objetivo por id */}
      <Text>Detalle de objetivo</Text>
    </View>
  );
}
