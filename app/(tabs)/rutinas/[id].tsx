// Componentes básicos de React Native
import { View, Text } from 'react-native';

// Pantalla de detalle de una rutina concreta (ruta dinámica /rutinas/[id])
export default function RutinaDetalleScreen() {
  return (
    // Contenedor provisional del detalle
    <View>
      {/* Placeholder hasta cargar datos de la rutina por id */}
      <Text>Detalle de rutina</Text>
    </View>
  );
}
