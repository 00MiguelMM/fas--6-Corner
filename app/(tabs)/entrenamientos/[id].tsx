// Componentes básicos de React Native
import { View, Text } from 'react-native';

// Pantalla de detalle de un entrenamiento concreto (ruta dinámica /entrenamientos/[id])
export default function EntrenamientoDetalleScreen() {
  return (
    // Contenedor provisional del detalle
    <View>
      {/* Placeholder hasta cargar datos del entrenamiento por id */}
      <Text>Detalle de entrenamiento</Text>
    </View>
  );
}
